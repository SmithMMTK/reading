
เมื่อคุณออกแบบ _scalable semantic model_ การเลือก _storage mode_ ที่เหมาะสมถือเป็นสิ่งสำคัญ ขึ้นอยู่กับแหล่งข้อมูลของคุณ คุณสามารถเลือกได้จาก 4 แบบ ได้แก่ _Import_, _DirectQuery_, _Direct Lake_, และ _Composite model_ ซึ่งแต่ละแบบมีข้อดีและข้อควรพิจารณาเพื่อให้ได้ประสิทธิภาพและการขยายตัวที่ดีที่สุด

## Import Mode
_Import_ คือการนำเข้าข้อมูลและเก็บไว้ภายใน _Power BI_ ซึ่งให้ความยืดหยุ่นมากที่สุดและประสิทธิภาพเร็วที่สุด อย่างไรก็ตามต้องมีการ _refresh_ ข้อมูลเป็นระยะ ๆ ทำให้ข้อมูลจะทันสมัยเท่ากับครั้งล่าสุดที่มีการ _refresh_

คำแนะนำสำหรับ _Import models_ สามารถนำไปใช้กับ _storage modes_ อื่นได้เช่นกัน ควรเลือกใช้ _Import_ ถ้ารูปแบบของโมเดลของคุณรองรับ เป้าหมายหลักคือการลดปริมาณข้อมูลที่นำเข้ามาใน _semantic model_ และลดจำนวนการแปลงข้อมูล (_transformation_) ที่เกิดขึ้นภายใน _Power BI_

เทคนิคการปรับแต่ง ได้แก่:

- เชื่อมต่อไปยัง _views_ แทนที่จะเป็น _tables_ เมื่อทำงานกับ _relational databases_
- รวมเฉพาะ _tables_, _rows_, และ _columns_ ที่จำเป็น
- พิจารณาใช้ _partitioning_ และ _incremental refresh_ เพื่อหลีกเลี่ยงการโหลดข้อมูลที่ไม่จำเป็น
- ใช้ _data types_ ที่เหมาะสม เช่นใช้ _integer_ แทน _string_ สำหรับ _ID columns_
- ตรวจสอบให้แน่ใจว่ามี _query folding_ เพื่อลดภาระของ _Power BI engine_

> 💡 เรียนรู้เทคนิคเพิ่มเติมเพื่อช่วยลดข้อมูลที่โหลดเข้าสู่ _import models_

## DirectQuery mode
_DirectQuery_ อนุญาตให้คุณ _query_ ข้อมูลจากแหล่งโดยตรงโดยไม่ต้องเก็บไว้ใน _Power BI_ เหมาะสำหรับการจัดการข้อมูลปริมาณมาก และการส่งข้อมูลแบบ _near real-time_ อย่างไรก็ตามประสิทธิภาพจะช้ากว่า _Import Mode_ และความสามารถในการทำ _modeling_ มีข้อจำกัด

เทคนิคการปรับแต่ง:

- หลีกเลี่ยงการคำนวณซับซ้อนในแหล่งข้อมูล โดยทำให้ _DAX_ ง่ายขึ้น
- ใช้ _dual storage mode_ สำหรับ _dimensions_ ที่เกี่ยวข้องกับ _fact tables_
- ใช้คุณสมบัติ _Assume Referential Integrity_ บนความสัมพันธ์
- หลีกเลี่ยงความสัมพันธ์ที่เกิดจาก _calculated columns_ และ _Unique Identifier columns_

> 💡 ดูเอกสารสำหรับคำแนะนำเพิ่มเติมเกี่ยวกับ _DirectQuery models_

## Direct Lake mode
_Direct Lake_ อนุญาตให้ _query_ ข้อมูลจาก _Microsoft Fabric lakehouse_ หรือ _warehouse_ โดยตรงโดยไม่เก็บไว้ใน _semantic model_ เหมาะกับการจัดการข้อมูลขนาดใหญ่ และถูกปรับแต่งให้โหลดข้อมูลจาก _Delta tables_ ใน _Microsoft Fabric_ ได้อย่างรวดเร็ว ข้อดีคือเข้าถึงข้อมูลขนาดใหญ่ได้อย่างรวดเร็ว แต่จำเป็นต้องมีการตั้งค่า _lakehouse_ หรือ _warehouse_ และอาจต้องปรับแต่ง _Delta tables_ เพื่อให้ได้ประสิทธิภาพสูงสุด

เทคนิคการปรับแต่ง:

- ตั้งค่า _Direct Lake_ ให้เข้าถึง _ADLS (Azure Data Lake Storage)_ ผ่าน _shortcuts_
- กำหนด _fallback behavior_ เป็น _DirectQuery_ สำหรับ _DAX queries_ ที่ซับซ้อน

## Composite mode
_Composite Mode_ สามารถผสมผสาน _Import_ และ _DirectQuery_ หรือรวมหลายแหล่ง _DirectQuery_ เข้าด้วยกัน รองรับ _many-to-many relationships_ โดยไม่ต้องใช้ _bridge tables_ ให้ความยืดหยุ่นทั้งด้าน _querying_ และ _reporting_ ด้วยการรวมข้อดีของทั้ง _Import_ และ _DirectQuery_ อย่างไรก็ตาม _Import tables_ ยังคงต้อง _refresh_ เป็นระยะ และอาจมีผลกระทบต่อประสิทธิภาพเมื่อรวมข้อมูลจากหลายแหล่ง

เทคนิคการปรับแต่ง:

- ตรวจสอบให้แน่ใจว่าแหล่งข้อมูล _back-end_ มีทรัพยากรเพียงพอ
- ลดจำนวน _literal values_ ใน _source queries_
- รักษาระดับ _cardinality_ ของคอลัมน์ที่ใช้ใน _relationships_ ให้ต่ำ

> 💡 ดูเอกสารสำหรับคำแนะนำเพิ่มเติมเกี่ยวกับ _Composite model_